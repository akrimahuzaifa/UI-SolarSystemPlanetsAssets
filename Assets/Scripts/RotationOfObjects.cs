using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RotationOfObjects : MonoBehaviour
{
/*    public float speed = 20f;

    GameObject cube0, cube1, sphere0;

    private void Start()
    {
        cube0 = GameObject.Find("Cube0");
        cube1 = GameObject.Find("Cube1");
        sphere0 = GameObject.Find("Sphere0");
    }

    // Update is called once per frame
    void Update()
    {
        //cube0.transform.rotation = Quaternion.Euler(cube0.transform.rotation.x, speed * Time.deltaTime, cube0.transform.rotation.z, Space.World);
        cube0.transform.Rotate(0, speed * Time.deltaTime, 0, Space.World);
        cube1.transform.Rotate(0, speed * Time.deltaTime, 0, Space.World);
        sphere0.transform.Rotate(0, speed * Time.deltaTime, 0);
    }

    public void AdjustSpeed(float newSpeed)
    {
        speed = newSpeed;
    }*/
}
