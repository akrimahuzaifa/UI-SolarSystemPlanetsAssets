using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VolumeOfObjects : MonoBehaviour
{
/*    public float volume = 1f;
    GameObject cube0, cube1, sphere0;

    private void Start()
    {
        cube0 = GameObject.Find("Cube0");
        cube1 = GameObject.Find("Cube1");
        sphere0 = GameObject.Find("Sphere0");
    }

    // Update is called once per frame
    void Update()
    {
        cube0.transform.localScale = new Vector3(volume, volume, volume);

        //cube0.transform.localScale(volume, volume, volume);

        *//*        cube0.transform.Rotate(0, volume * Time.deltaTime, 0);
                cube1.transform.Rotate(0, volume * Time.deltaTime, 0);
                sphere0.transform.Rotate(0, volume * Time.deltaTime, 0);*//*
    }
    public void AdjustVolume(float newVolume)
    {
        volume = newVolume;
    }*/
}
